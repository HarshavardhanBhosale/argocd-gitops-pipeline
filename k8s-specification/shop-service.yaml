apiVersion: v1
kind: Service
metadata:
  name: shopping-app-service
  namespace: shopping-app
spec:
  type: NodePort
  selector:
    app: myshop
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
